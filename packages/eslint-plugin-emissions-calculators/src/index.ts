import { requireOptionalZodTransformGuard } from './rules/require-optional-zod-transform-guard';

export const rules = {
  'require-optional-zod-transform-guard': requireOptionalZodTransformGuard,
};

export default { rules };
